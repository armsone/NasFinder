# NasFinder 작업 보존 및 배포 규칙

이 저장소에서 소스 수정, GitHub 푸시, 실기기 설치를 수행하는 모든 에이전트는 다음 규칙을 지켜야 한다.

1. `./scripts/repository_guard.sh status`는 Git 보존·복구, 설치, worktree 전환 또는 배포를 실제로 수행하기 직전과 완료 후에만 실행한다. 읽기 전용 조사와 일반 소스 수정 시작·종료마다 관행적으로 실행하지 않는다.
2. 위 작업을 위해 확인했을 때 `refs/codex/snapshots`에 일반 로컬 브랜치가 포함하지 않는 스냅샷이 있으면 해당 외부 작업만 중단하고 복구 필요성을 보고한다. 안전한 로컬 조사와 수정까지 모두 중단하지 않는다.
3. 사용자가 받을 최종 앱을 `xcodebuild`와 `devicectl`로 직접 설치하지 않는다.
4. 사용자가 최종 앱 설치를 명시적으로 요청하면 변경 위험도에 맞는 검증을 통과한 동일 산출물을 `make install-verified DEVICE_ID=<id> TEST_ONLY=<test-selector>`로 설치한다. 전체 테스트가 실제로 필요한 경우에만 `FULL_TESTS=1`을 사용한다. 설치 요청은 커밋·푸시를 포함하지 않는다.
5. 사용자가 커밋·푸시도 함께 요청한 경우에만 최종 설치 전 현재 브랜치, HEAD와 upstream 상태를 한 번 확인해 알린다.
6. 작업 폴더가 dirty한 상태에서는 push하지 않는다. 저장소의 pre-push 훅을 우회하는 `--no-verify`를 사용하지 않는다.
7. 사용자가 archive, cleanup 또는 다른 worktree 전환을 요청하면 변경 보존 상태를 확인한다. GitHub 푸시는 별도 요청이 있을 때만 수행한다.
8. 테스트·빌드·아이콘 번들 검증·설치·실행 중 하나라도 실패하면 완료라고 보고하지 않는다.
9. iOS 시뮬레이터를 기본 검증 수단으로 실행하지 않는다. 시뮬레이터에서만 재현되는 UI·기기 조합을 확인하거나 실기기에서 동등한 검증을 할 수 없는 경우에만 사용하고, 사용 이유를 진행 상황에 알린다.
10. 실기기가 연결된 작업에서는 빌드·테스트·설치 검증을 해당 실기기로 수행한다. `make install-verified`의 테스트 대상도 기본적으로 설치 대상 실기기와 같아야 한다.
11. 일반적인 UI·문구·색상·레이아웃 및 국소 기능 변경은 변경과 직접 관련된 테스트와 빌드 성공을 확인한 뒤 설치할 수 있다.
12. 네트워크, 인증, 파일 삭제·이동, 캐시, 영상 재생 엔진처럼 영향도가 큰 변경은 관련 회귀 테스트 범위를 확대한다.
13. 전체 실기기 테스트는 TestFlight·정식 배포 전, 여러 핵심 모듈을 동시에 변경한 경우, 공통 기반 코드·외부 의존성을 변경한 경우, 관련 테스트만으로 영향 범위를 판단하기 어려운 경우 또는 사용자가 요청한 경우에만 실행한다.
14. 반복 수정·설치 과정에서는 관련 테스트와 빌드 검증만 수행하고, 전체 테스트는 최종 배포 후보에서 한 번 실행한다.
15. 완료 보고에는 실행한 테스트 범위와 생략한 검증을 명확히 적는다.
16. 작은 수정마다 빌드·테스트하지 않고, 컴파일과 검증이 가능한 하나의 의미 있는 변경 묶음이 완성된 뒤 검증한다.
17. 변경 전 기준 테스트는 버그 재현, 현재 기준 상태가 불명확한 경우 또는 공통 기반 변경에만 실행한다. 동일한 소스 상태·테스트 선택자·환경에서 통과한 검증은 반복하지 않는다.
18. 검증은 관련 테스트에서 시작해 빌드와 필요한 실기기 확인으로 넓힌다. 실패 수정 후에는 실패 항목과 직접 영향 범위만 다시 검증한다.
19. 같은 인프라 오류가 두 번 반복되면 같은 명령을 다시 실행하지 않고 원인을 진단한다.
20. 시뮬레이터 테스트가 예외적으로 필요하면 `-parallel-testing-enabled NO -maximum-parallel-testing-workers 1`을 사용하고 동시에 하나의 destination만 실행한다.
21. 시뮬레이터 테스트 전후 `~/Library/Developer/XCTestDevices`의 복제 기기 수를 확인한다. 새 복제본이 남으면 추가 테스트를 중단하고, 실행 중인 테스트가 없는지 확인한 뒤 정확한 생성 대상을 보고하고 정리 승인을 받는다.
22. 여러 에이전트나 작업이 같은 Xcode 프로젝트, CoreSimulator 또는 실기기를 동시에 사용하지 않는다.
23. 빌드·테스트 전에 데이터 볼륨의 여유 공간을 확인한다. 50GiB 미만이거나 전체의 10% 미만이면 새 대형 빌드·전체 테스트·시뮬레이터 생성을 시작하지 않는다.
24. 작업 전용 DerivedData와 결과 번들은 성공·실패·중단 후 더 이상 필요 없으면 정리한다. 사용자 공용 DerivedData·캐시나 다른 작업 산출물은 삭제하지 않는다.
25. Mac의 디스크, 메모리, CPU, 배터리, 스왑, 시뮬레이터와 연결 기기는 보호 대상이다. 장시간 고부하와 무제한 프로세스·복제 기기·로그·캐시 누적을 허용하지 않는다.
26. 진행 확인이나 상태 보고만을 위해 테스트·빌드·설치를 실행하지 않고, 이미 확인한 결과를 재사용한다.
27. 명령줄 `xcodebuild`는 `./scripts/xcodebuild_project.sh`를 통해 실행해 Swift 패키지 체크아웃과 캐시를 저장소의 `.build` 아래에서 재사용한다. 사용자 홈의 Xcode·SwiftPM 패키지 캐시에 의존하지 않는다. 래퍼는 같은 저장소의 패키지 해석·빌드를 잠금으로 직렬화하므로 대기 중인 명령을 중복 실행하지 않는다.
28. `Package.resolved`가 바뀌었거나 프로젝트 로컬 패키지 캐시가 없을 때만 `make packages`를 실행한다. Xcode의 SwiftPM 해석은 내부에서 `sandbox-exec`를 다시 사용하므로 Codex 샌드박스 안에서 먼저 실패시키지 말고, 이 명령 하나에만 범위를 제한해 샌드박스 밖에서 한 번 실행한다. 같은 네트워크·중첩 샌드박스 오류를 반복하지 않는다.
29. 패키지 해석이 중단되면 소스 컴파일 전 단계에서 멈춘 것으로 보고한다. 소스 오류가 없다고 단정하지 말고, 패키지 오류와 소스 검증 상태를 분리한다.

## UI 디자인 원칙

- 필요한 정보와 기능은 찾는 사람에게는 명확히 발견되어야 하지만, 찾지 않는 사람의 시선을 빼앗아서는 안 된다.
- 핵심 행동만 전면에 두고 설명, 상태, 드문 행동은 작은 글자·낮은 대비·보조 메뉴처럼 한 단계 물러난 표현을 우선한다.
- 보조 정보는 읽을 수 없게 숨기지 않되 화면의 시각적 위계를 해치지 않도록 짧고 조용하게 표현한다.
- 기능을 화면에서 덜어낼 때 기존 기능 자체가 사라지지 않도록, 필요한 경우 `…` 메뉴 같은 발견 가능한 보조 경로를 유지한다.

로컬 훅은 저장소를 처음 받은 뒤 `make hooks`로 활성화한다.
