# NasFinder 작업 보존 및 배포 규칙

이 저장소에서 소스 수정, GitHub 푸시, 실기기 설치를 수행하는 모든 에이전트는 다음 규칙을 지켜야 한다.

1. 작업 시작과 종료 시 `./scripts/repository_guard.sh status`를 실행한다.
2. `refs/codex/snapshots`에 일반 로컬 브랜치가 포함하지 않는 스냅샷이 있으면 작업을 중단하고 먼저 복구한다.
3. 사용자가 받을 최종 앱을 `xcodebuild`와 `devicectl`로 직접 설치하지 않는다.
4. 최종 앱은 변경을 모두 커밋하고 GitHub upstream에 동일한 커밋을 푸시한 후, 변경 위험도에 맞는 실기기 검증을 통과한 경우에만 `make install-verified DEVICE_ID=<id> TEST_ONLY=<test-selector>`로 설치한다. 전체 테스트가 필요한 경우에는 `FULL_TESTS=1`을 사용한다.
5. 최종 설치 전에 현재 브랜치, HEAD 커밋, upstream 커밋을 사용자에게 진행 상황으로 알린다.
6. 작업 폴더가 dirty한 상태에서는 push하지 않는다. 저장소의 pre-push 훅을 우회하는 `--no-verify`를 사용하지 않는다.
7. 작업을 archive, cleanup 또는 다른 worktree로 전환하기 전에 변경이 일반 브랜치와 GitHub에 보존됐는지 확인한다.
8. 테스트·빌드·아이콘 번들 검증·설치·실행 중 하나라도 실패하면 완료라고 보고하지 않는다.
9. iOS 시뮬레이터를 기본 검증 수단으로 실행하지 않는다. 시뮬레이터에서만 재현되는 UI·기기 조합을 확인하거나 실기기에서 동등한 검증을 할 수 없는 경우에만 사용하고, 사용 이유를 진행 상황에 알린다.
10. 실기기가 연결된 작업에서는 빌드·테스트·설치 검증을 해당 실기기로 수행한다. `make install-verified`의 테스트 대상도 기본적으로 설치 대상 실기기와 같아야 한다.
11. 일반적인 UI·문구·색상·레이아웃 및 국소 기능 변경은 변경과 직접 관련된 테스트와 빌드 성공을 확인한 뒤 설치할 수 있다.
12. 네트워크, 인증, 파일 삭제·이동, 캐시, 영상 재생 엔진처럼 영향도가 큰 변경은 관련 회귀 테스트 범위를 확대한다.
13. 전체 실기기 테스트는 TestFlight·정식 배포 전, 여러 핵심 모듈을 동시에 변경한 경우, 공통 기반 코드·외부 의존성을 변경한 경우, 관련 테스트만으로 영향 범위를 판단하기 어려운 경우 또는 사용자가 요청한 경우에만 실행한다.
14. 반복 수정·설치 과정에서는 관련 테스트와 빌드 검증만 수행하고, 전체 테스트는 최종 배포 후보에서 한 번 실행한다.
15. 완료 보고에는 실행한 테스트 범위와 생략한 검증을 명확히 적는다.

## UI 디자인 원칙

- 필요한 정보와 기능은 찾는 사람에게는 명확히 발견되어야 하지만, 찾지 않는 사람의 시선을 빼앗아서는 안 된다.
- 핵심 행동만 전면에 두고 설명, 상태, 드문 행동은 작은 글자·낮은 대비·보조 메뉴처럼 한 단계 물러난 표현을 우선한다.
- 보조 정보는 읽을 수 없게 숨기지 않되 화면의 시각적 위계를 해치지 않도록 짧고 조용하게 표현한다.
- 기능을 화면에서 덜어낼 때 기존 기능 자체가 사라지지 않도록, 필요한 경우 `…` 메뉴 같은 발견 가능한 보조 경로를 유지한다.

로컬 훅은 저장소를 처음 받은 뒤 `make hooks`로 활성화한다.
